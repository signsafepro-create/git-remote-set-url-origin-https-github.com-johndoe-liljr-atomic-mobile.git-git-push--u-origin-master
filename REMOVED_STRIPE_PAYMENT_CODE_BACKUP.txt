// Backup of removed Stripe/payment code from Lil Jr project (April 2026)
// Restore these snippets if payment/tier activation is needed in the future.

// --- PricingScreen.js (activateTier, payment logic)
// const activateTier = async (tier) => {
//   if (!email.includes('@')) {
//     Alert.alert('Email required', 'Please enter a valid email to activate.');
//     return;
//   }
//   setLoading(tier.key);
//   try {
//     const { url } = await api.createCheckout(tier.key, email);
//     Alert.alert('Checkout ready', url || 'Checkout session created.');
//   } catch {
//     Alert.alert('Unavailable', 'Checkout endpoint is not active yet on this backend.');
//   } finally {
//     setLoading(null);
//   }
// };

// --- api/client.js (createCheckout)
// async createCheckout(tier, email) {
//   return this.post('/create-checkout', { tier, email });
// },

// --- config/api.js (checkout endpoint)
// checkout: '/create-checkout',

// --- .env and docs
// STRIPE_SECRET_KEY=your_stripe_key
// STRIPE_WEBHOOK_SECRET=your_webhook_secret

// --- server.js (Stripe endpoint)
// app.post('/create-checkout', ...)

// --- BACKEND_HANDOFF.md
// Stripe/payment instructions and env vars

// --- Any other payment/tier activation logic
// Restore as needed.
